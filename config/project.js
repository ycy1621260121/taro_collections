module.exports = {
  projectName: 'testB', //项目为testA，在这里改为testA即可
  testA_appid: 'wxc1bc398b8f605514', //不同项目配置不同的appid，第一次配置完了即可，以前的保留，每次启动改上面的项目名称即可
  testB_appid: 'wxe10982070733ace7' //规范： 项目名称_appid（必须）
}
